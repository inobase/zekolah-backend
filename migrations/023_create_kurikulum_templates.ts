// =====================================================
// Migration: Create kurikulum_templates table
// PLACEHOLDER — This migration was previously run but file missing.
// Phase 3 was later SKIPPED, so this remains empty.
// =====================================================

import { Knex } from 'knex';

export async function up(knex: Knex): Promise<void> {
  // Placeholder — migration already applied
}

export async function down(knex: Knex): Promise<void> {
  // Placeholder — migration already applied
}
